# Auto generated color theme for image at: [Local wallpaper]
general {
    col.active_border = rgba(5c113bFF)
    col.inactive_border = rgba(59404aCC)
}

plugin {
    droidbars {
        # example config
        bar_height = 30
        background_color = rgba(130F10FF)
        # background_color_active = rgba(514348FF) # Not added yet
        text_color = rgba(fed9e6FF)
        font_family = Lexend
        button_font_fmily = JetBrainsMono NF

        # example buttons (R -> L)
        # droidbars-button = [0]isLeft(0/1), [1]color, [2]color2, [3]width, [4]height, [5]icon, [6]buttonType, [7]on-click     
        droidbars-button = 0, rgba(fed9e6FF), rgba(ffafd2FF), 42, 16,‎    󰖭, normal, hyprctl dispatch killactive
        droidbars-button = 0, rgba(fed9e6FF), rgba(ffafd2FF), 42, 16,‎   , normal, hyprctl dispatch fullscreen 1
        droidbars-button = 1, rgba(fed9e6FF), rgba(ffafd2FF), 16, 16,‎󰐃, pin, hyprctl dispatch pin
        droidbars-button = 1, rgba(fed9e6FF), rgba(ffafd2FF), 16, 16,󰗘, float, hyprctl dispatch togglefloating
    }
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgb(130F10)
        col.text = rgb(ebe0e2)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(ebe0e2), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(ebe0e2), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(ebe0e2), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}
